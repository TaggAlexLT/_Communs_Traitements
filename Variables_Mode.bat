if %MODE% NEQ PROD SET PRINT=-OnLine
if %MODE% EQU PROD SET PRINT=-OnLine
if %MODE% NEQ PROD SET MODE_PRINT=
if %MODE% EQU PROD SET MODE_PRINT=
if %MODE% NEQ PROD SET CREATEMODE=Test
if %MODE% EQU PROD SET CREATEMODE=Real
if %MODE% NEQ PROD SET PRODINFOMODE=TEST
if %MODE% EQU PROD SET PRODINFOMODE=PROD
if %MODE% NEQ PROD SET EDC_MODE=TEST
if %MODE% EQU PROD SET EDC_MODE=REAL
