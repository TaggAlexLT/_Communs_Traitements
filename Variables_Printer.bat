if %PRINTER% EQU XEIKON_8000   SET IDPRINTER=26
if %PRINTER% EQU IMPIKA        SET IDPRINTER=27
if %PRINTER% EQU XEIKON        SET IDPRINTER=49
if %PRINTER% EQU VARIJET       SET IDPRINTER=20
if %PRINTER% EQU DIGIFLEX      SET IDPRINTER=32
if %PRINTER% EQU KONICA_BH     SET IDPRINTER=33
if %PRINTER% EQU KONICA        SET IDPRINTER=46
if %PRINTER% EQU RISO          SET IDPRINTER=43
if %PRINTER% EQU PAGE_A3       SET IDPRINTER=43
if %PRINTER% EQU PAGE_A4       SET IDPRINTER=43
if %PRINTER% EQU SCREEN        SET IDPRINTER=44
if %PRINTER% EQU SCREEN_NX     SET IDPRINTER=51

if %PRINTER% EQU XEIKON_8000  SET RESOLUTION=600
if %PRINTER% EQU IMPIKA       SET RESOLUTION=600
if %PRINTER% EQU XEIKON       SET RESOLUTION=600
if %PRINTER% EQU VARIJET      SET RESOLUTION=300
if %PRINTER% EQU DIGIFLEX     SET RESOLUTION=600
if %PRINTER% EQU KONICA_BH    SET RESOLUTION=600
if %PRINTER% EQU RISO         SET RESOLUTION=300
if %PRINTER% EQU KONICA       SET RESOLUTION=1200
if %PRINTER% EQU PAGE_A3      SET RESOLUTION=600
if %PRINTER% EQU PAGE_A4      SET RESOLUTION=600
if %PRINTER% EQU SCREEN       SET RESOLUTION=1200
if %PRINTER% EQU SCREEN_NX    SET RESOLUTION=600
